node_modules/
.env
.env.local
.vercel

# Next
.next/
next-env.d.ts
tsconfig.tsbuildinfo

# Copia del legacy que servía el iframe. Ya no se genera (jul-2026: el shell sirve
# todas las secciones); queda ignorada por si alguien tiene la carpeta vieja local.
public/legacy/

# Fixture del test de paridad del ETL: son ventas reales bajadas de Supabase.
# Se regenera con `npm run fixture-etl`.
tests/fixtures/

# Reporte de cobertura: se genera con `npx vitest run --coverage`.
coverage/
